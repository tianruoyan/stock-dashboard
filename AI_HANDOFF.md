# V2 Codex 最小交接

先读父目录 `AGENTS.md`、本文件，再只读与任务相关的一个配置、模块和测试。

默认约束：

- 这是 shadow，不晋升、不回写 V1、不交易、不改用户资产。
- 运行镜像 `/Users/sweet_orange/stock-dashboard-v2-local` 不是源码真相源。
- 新研究逻辑必须保留事实、推断、交易含义和反证；缺数据时降级。
- 改阈值优先改配置；改代码必须补单测。

最小维护模板：

```text
范围：V2 shadow 的 <一个模块>
问题：<一个复现>
允许修改：<一个配置/模块/测试>
禁止修改：V1、用户资产、晋升状态、交易能力
验收：python3 -m unittest <具体测试>
部署：<一个部署脚本> + 8878 健康检查
```

不要为了理解系统扫描全部 `data/v2/`、历史报告和 `v2_acceptance_screenshots/`。先运行具体测试，只有失败指向其他模块时再扩展上下文。
